<!DOCTYPE html>
<html lang="en">

<!--------------------------------------------------head------------------------------------------------------------->
<head>
    
    <%- include('./partials/head') %>
    
    <link rel="stylesheet" href="/css/register.css">
    <title>About-us</title>
</head>
<!--------------------------------------------------body------------------------------------------------------------->
 <body>
<!-------------------------------------------------header------------------------------------------------------------> 
<header>
    
    <%- include('./partials/header') %>
    
         
</header>

<!-------------------------------------------------main------------------------------------------------------------>    
    <main>
        <h1 class="titular-uno"><strong>¡Bienvenidos!</strong></h1>
        <section class="section-registro">
            <article class="registro-uno">
            <p class="registrarme"><strong>Registrarme con:</strong></p>
            <a href="https://www.facebook.com/"><i class="fab fa-facebook-square button"></i></a>
            <a href="https://myaccount.google.com/"><i class="fab fa-google button"></i></a>
            </article>
            <section class="registro-dos">
                <form class="form-registro" action="/user/register" method="POST" enctype="multipart/form-data">

                    <h3 class="titular-dos"><strong>Formulario de registro</strong></h3>
                    
                    <fieldset>
                        
                    <label>Nombre:</label>
                    <input type="text" name="firstName" id="firstName" placeholder="Nombre" class= "usuarioNombre" value="<%=(locals.errors && locals.old) ? old.firstName : "" %>">
                    <p class="mensajeFirstName"> </p>
                    <% if (locals.errors && errors.firstName) { %>
                        <p id= "text-danger">
                            <%= errors.firstName.msg %> 
                        </p>
                       <% } %>
                    
                    <label>Apellido:</label>
                    <input type="text" name="lastName" id="lastName" placeholder="Apellido" class= "usuarioNombre"  value="<%= (locals.errors && locals.old) ? old.lastName : "" %>">
                    
                    <% if (locals.errors && errors.lastName) { %>
                        <p id= "text-danger">
                            <%= errors.lastName.msg %> 
                        </p>
                       <% } %>

                    <label for="avatar">Seleccionar imagen de perfil</label>
                    <input type="file" name="avatar" id="avatar" class= "usuarioNombre">

                    <% if (locals.errors && !errors.avatar) { %>
                        <p id="text-danger">
                            Debes seleccionar una imagen de perfil nuevamente
                        </p>
                       <% } %>

                    <% if (locals.errors && errors.avatar) { %>
                        <p id="text-danger">
                            <%= errors.avatar.msg %> 
                        </p>
                       <% } %>

                    <label>Email:</label>
                    <input type="email" name="email" id="email" placeholder= "Email" class="usuarioNombre"  value=" <%=(locals.errors && locals.old) ? old.email : "" %>">

                    <% if (locals.errors && errors.email) { %>
                        <p id="text-danger">
                            <%= errors.email.msg %> 
                        </p>
                       <% } %>
           
                    <label>Fecha de Nacimiento:</label>
                    <input class="fecha-nac" type="date" name="birthDate" value="" min="1930-01-01" max="2019-12-30">

                    <% if (locals.errors && !errors.birdthDate) { %>
                        <p id="text-danger">
                            Debes seleccionar tu fecha de nacimiento nuevamente
                        </p>
                       <% } %>

                    <% if (locals.errors && errors.birdthDate) { %>
                        <p id="text-danger">
                            <%= errors.birthDate.msg %> 
                        </p>
                       <% } %>
                    
                    <label>Domicilio:</label>
                    <input type="text" name="adress" id="adress" placeholder="Dirección" class="usuarioNombre"  value="<%=(locals.errors && locals.old) ? old.adress : "" %>">

                    <% if (locals.errors && errors.adress) { %>
                        <p id= "text-danger">
                            <%= errors.adress.msg %> 
                        </p>
                       <% } %>

                    <label>Número de contacto:</label>
                    <input type="tel" name="phoneNumber" id="phoneNumber" placeholder="Telefono" class= "usuarioNombre"  value="<%= (locals.errors && locals.old) ? old.phoneNumber : "" %>">

                    <% if (locals.errors && errors.phoneNumber) { %>
                        <p id= "text-danger">
                            <%= errors.phoneNumber.msg %> 
                        </p>
                       <% } %>

                    
                    <label>País:</label>
                    <input type="text" name="country" id="country" placeholder="Pais" class="usuarioNombre" 
                    value="<%= (locals.errors && locals.old) ? old.country : "" %>">

                    <% if (locals.errors && errors.country) { %>
                        <p id= "text-danger">
                            <%= errors.country.msg %> 
                        </p>
                       <% } %>
                    
                    <label>Contraseña:</label>
                    <input type="password" name="password" id="password" placeholder="Contraseña" class="usuarioNombre">

                    <% if (locals.errors && errors.password) { %>
                        <p id= "text-danger">
                            <%= errors.password.msg %> 
                        </p>
                       <% } %>

                    <label>Confirmar contraseña:</label>
                    <input type="password" name="passwordConfirm" id="passwordConfirm" placeholder="Confirmar contraseña" class="usuarioNombre">
                    </fieldset>

                    <% if (locals.errors && errors.passwordConfirm) { %>
                        <p id= "text-danger">
                            <%= errors.passwordConfirm.msg %> 
                        </p>
                       <% } %>

                    <fieldset class="opciones-pago">
                        <h3 class="opciones">Seleccionar opciones de contribución</h3>

                        <% if (locals.errors && errors.medio_de_pago) { %>
                            <p id= "text-danger">
                                <%= errors.medio_de_pago.msg %> 
                            </p>
                           <% } %>

                        <% for( let i = 0; i < mediosDePago.length; i++ ) { %>
                            
                            <label for="">

                            <input class="forma-pago" type="checkbox" name="medio_de_pago" value= "<%= mediosDePago[i].id %>"> <%= mediosDePago[i].medio_de_pago %>
                            
                            </label>
                             
                        <% } %>

                    </fieldset>


                <a href="/"><input class="button-dos" type="submit" value="Registrarme"></a>    
                </form>
                
            </section>

        </section>
    </main>
<!--------------------------------------------------footer------------------------------------------------------------->
    <footer>

        <%- include('./partials/footer') %>

    </footer>

    <script src="/js/register.js">

    </script>
</body>

</html>